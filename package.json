{
  "name": "directus-example",
  "private": true,
  "scripts": {
    "start": "npx directus start",
    "assume-unchanged": "git update-index --assume-unchanged data.db",
    "postinstall": "npm run assume-unchanged"
  },
  "dependencies": {
    "directus": "9.12.2",
    "sqlite3": "^5.0.8"
  }
    "engines": {
    "node": ">=12.x.x <=16.x.x",
    "npm": ">=6.0.0"
  },
}
